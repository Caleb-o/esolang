proc fib(n : int) -> int {
	n 1 <= if {
		n
	} else {
		n n 0

		true loop {
			bind | idx |

			
			1 - swap
			2 - swap

			idx 1 + n <

			rot drop swap
		}
	}
}
	
proc main() -> void {
	# 1 2 3
	# bind | a, b, c |
	# a println drop
	# b println drop
	# c println drop
	'fib = ' print drop
	| 3 | !fib println drop
}