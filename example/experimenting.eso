using 'fib'

proc test(a : int) -> void {
	a println drop
	'test' println drop
}

proc test(a, b : int) -> void {
	a println drop
	b println drop
	'test2' println drop
}
	
proc main() -> void {
	5

	true loop {
		dup |!1| !test
		
		1 -
		dup 0 >
	}

	drop
}