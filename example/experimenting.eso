proc fib(n : int) -> int {
	n 1 <= if {
		n
	} else {
		n n 0

		true loop {
			bind idx

			1 - swap
			2 - swap

			idx 1 + n <

			rot drop swap
		}
	}
}
	
proc main() -> void {
	| 7 | !fib println drop
}