proc test(x, y, z : int) -> void {
	'Test Func' println drop
}

# Entry point
proc main() -> void {
	1 2 3 !test
	drop drop drop
}

# Error: We redefine a proc here
# proc main(a, b, c : int) -> void {}